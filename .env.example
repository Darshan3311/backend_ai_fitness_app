# Copy to .env (do NOT commit .env)
# Production values should be set in Render dashboard, not here.

MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.kpm1vpd.mongodb.net/fitnessapp?retryWrites=true&w=majority
JWT_SECRET=change-this-to-a-long-random-string
GEMINI_API_KEY=
ALLOWED_ORIGINS=https://fitnessapp-frontend.onrender.com
# Optional override for local dev
PORT=8081

# Local-only example (uncomment if you run local Mongo)
# MONGODB_URI=mongodb://localhost:27017/fitnessapp

